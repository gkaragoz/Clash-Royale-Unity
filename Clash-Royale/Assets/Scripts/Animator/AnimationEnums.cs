namespace Custom {
    public enum AnimationType {
        Idle,
        Run,
        Attack
    }

    public enum AnimationDirection {
        UpperLeft,
        Up,
        UpperRight,
        Right,
        BottomRight,
        Bottom,
        BottomLeft,
        Left
    }
}
